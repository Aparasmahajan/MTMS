node_modules/
.next/
out/
# The static client demo, and the API routes while `build:demo` has them parked.
demo/
.api-parked/
*.tsbuildinfo
next-env.d.ts
.env
.env.local

# The file-backed store. Delete it to reseed from lib/server/seed.ts.
data/tracker.json
data/tracker.json.tmp

# Python bytecode from running the drift agent locally.
__pycache__/
*.pyc
