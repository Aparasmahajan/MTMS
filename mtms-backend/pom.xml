<?xml version="1.0" encoding="UTF-8"?>
<!--
  MTMS API — the Spring Boot service.

  Every version below is pinned to something already present in the local Maven
  repository. This machine reaches neither Maven Central nor the Nokia mirror, so the
  build runs offline (see .mvn/maven.config) and a version bump is not a free action:
  check ~/.m2 before changing one.

  Two deliberate departures from the design bundle, both load-bearing:

  - starter-jdbc, not starter-data-jdbc. The schema is hand-written and the projection
    is a small number of indexed reads that we want to spell out. Spring Data JDBC's
    repository abstraction would sit between us and SQL we already know we want. It is
    also the one starter absent from the local repository.
  - Java 21, not 17. Records carry the domain model, sealed interfaces carry the
    command types, and pattern-matched switch makes the projection readable.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>io.mtms</groupId>
  <artifactId>mtms-api</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <name>MTMS API</name>
  <description>Organization Management System — REST API</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jjwt.version>0.12.6</jjwt.version>
    <springdoc.version>2.6.0</springdoc.version>
    <resilience4j.version>2.1.0</resilience4j.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>

    <!--
      Mail. Invitations and password resets are single-use links, and until this was added the
      only way to deliver one was to read it off the screen and send it by hand — which meant
      every new account depended on an administrator being at a keyboard.

      It stays inert until mtms.mail.host is set, so a deployment without a relay behaves exactly
      as before rather than failing to start. See SmtpMailer.

      Noted because an older comment in WebhookNotifier says this dependency could not be
      fetched: that was true while the build ran with -o against Nokia's repository. It builds
      against ~/.m2p now, online, and the artifact resolves.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>
    <!--
      Spring Security's SCryptPasswordEncoder delegates to BouncyCastle. It is here so the
      Java service hashes passwords the same way lib/server/auth.ts does, rather than
      substituting bcrypt for the sake of one fewer dependency.
    -->
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>1.78</version>
    </dependency>

    <!-- The relational target. Schema lives in db/migration, not in annotations. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>8.4.0</version>
      <scope>runtime</scope>
      <exclusions>
        <!--
          protobuf-java is a hard dependency of the connector, but only the X DevAPI uses
          it and this service speaks plain JDBC. Excluding it keeps a 1.7 MB jar nothing
          calls out of the build. If anyone ever reaches for the X Protocol, this is why
          it fails at class-load rather than at compile.
        -->
        <exclusion>
          <groupId>com.google.protobuf</groupId>
          <artifactId>protobuf-java</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <!--
      Flyway 10 split each database into its own module. Without this one, Flyway starts
      and then fails at the first migration with "no database plugin found" rather than at
      configuration time, which is a confusing way to learn the dependency is missing.
    -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>

    <!-- The projection cache seam. Optional at runtime; see CacheConfiguration. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-cache</artifactId>
    </dependency>

    <!-- The outbox drain. Optional at runtime; see EventsConfiguration. -->
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka</artifactId>
    </dependency>

    <!-- Access tokens. Refresh tokens are opaque and live in the database. -->
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- Health, metrics, rate limiting. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.resilience4j</groupId>
      <artifactId>resilience4j-spring-boot3</artifactId>
      <version>${resilience4j.version}</version>
    </dependency>

    <!-- Serves contracts/openapi.yaml as live documentation at /swagger-ui.html. -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Integration tests run on H2 in PostgreSQL mode, so `mvn test` needs no daemon. -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>2.2.224</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
